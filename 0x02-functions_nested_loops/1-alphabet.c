/**
 * main - Entry point
 * Description: prints alphabets to console.
 * Return: Always 0 (success)
 */

#include <stdio.h>
#include "main.h"
int main(void)
{
void print_alphabet(void)
{
char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		_putchar(letter);

	_putchar('\n')
}


return (0);
}
